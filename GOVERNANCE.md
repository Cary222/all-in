# All In 项目治理

本文档说明 All In 的维护责任、权限边界和代码合入规则。

All In 允许 AI 辅助实现、测试和整理代码，但维护权限、风险判断、审批和发布责任由明确的人类维护者承担。

## 治理原则

- 用户安全、人工确认和隐私保护优先于功能数量与开发速度。
- 任何改动都不应降低默认的发送保守程度或削弱人工确认环节。
- 对默认分支的修改通过 Pull Request 完成，并必须通过 CI。
- AI 工具和机器人账号不承担审批或发布责任。

## 高风险边界

以下改动按高风险处理，合并前必须明确复核安全边界：

- 取消或绕过人工确认，提高默认发送频率，放宽发送时间窗口、节流或账号风险限制。
- 处理 API Key、登录凭据、简历和其他隐私数据，或改变其读取、保存、上传和外发方式。
- 修改发送、浏览器控制、平台能力开关、数据库迁移、任务整体暂停/恢复和错误恢复边界。
- 修改 CI、发布、依赖锁定、仓库权限或本治理规则。
- 其他可能导致批量操作、数据丢失、隐私泄露、账号封禁或供应链风险的改动。

## 权限级别

| 身份 | 建议 GitHub 权限 | 可以做什么 |
|---|---|---|
| 社区贡献者 | Read | 查看和讨论项目，通过 Fork 提交 Issue 和 PR |
| 维护者 | Admin / Write | 维护、审核和合并全项目改动，负责发布与安全复核 |

不应绕过受保护分支。紧急安全修复也必须在事后补充 PR、原因、影响和验证结果。

## 代码合入流程

1. 从 `main` 创建分支，提交改动。
2. 推送并创建 Pull Request，说明改动原因和影响范围。
3. 相关测试与必需检查通过。
4. 高风险改动完成安全边界复核后合并；确认 GitHub 状态为 `MERGED`。

不使用 `--admin` 或等效方式绕过检查，也不降低审核或仓库保护要求。

## 规则变更

本文件和 `.github/CODEOWNERS` 的修改通过独立 PR 完成，需说明变更原因和权限影响。治理 PR 不得夹带功能或运行逻辑改动。
